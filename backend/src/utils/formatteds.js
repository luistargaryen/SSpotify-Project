"use strict";
//Este es un ejemplo de Spotify para formatear canciones, verifica que funcione bien
export const formattedTrack = (tracks) => {
  try {
  } catch (error) {
    console.log(`Hubo un error al formatear la cancion ${error.message}`);
  }
};
